---
applyTo: ".github/workflows/**/*.yml,.github/workflows/**/*.yaml,.github/actions/**/*.yml"
---

Follow the canonical instructions in `AGENTS.md` (Non-negotiable invariants, Safe
implementation patterns) for this file. Load a matching skill from `.agents/skills/`
when the change touches its scope.
