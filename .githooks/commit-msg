#!/bin/sh
subject="$(head -1 "$1")"
printf '%s\n' "$subject" | grep -Eq '^(feat|fix|docs|test|refactor|perf|build|ci|chore)(\([a-z0-9-]+\))?!?: .+' || {
  echo "Commit subject must follow Conventional Commits." >&2
  exit 1
}
